// node js -> instalação no site https://nodejs.org/pt/download

// Consultar a versão da Node.js: node -v

// npm install -y -> para instalar o npm
// Consultar a versão da npm: npm -v
// npm init -y -> para gerar um arquivo package.json

// npm install http express debug --save -> instalação de 
// 3 pacotes (http, express, debug)

// npm install body-parser --save -> instala a dependencia body-parser
// responsável por formatar a saida para o formato json

// -> npm install
// "npm run dev "-> COMANDO NPM QUE INICIALIZA O SERVIDOR E A API COMEÇA A RODAR

// npm install @prisma/client -> Instalação da prisma

// npx prisma migrate dev -> cria um arquivo de migração para o bd
// npx prisma migrate reset -> reseta a migração(banco de dados é perdido e é necessário criar outro.)